function text_create(src,evnt)
% src - the object that is the source of the event 
% evnt - empty for this property
%          set(gcbf,'FontSize',20);

% attempt to set default font size for xlabels
% to call for every created text object, run: set(0,'DefaultTextCreateFcn',@text_create)
        %get(gcbf);
end
